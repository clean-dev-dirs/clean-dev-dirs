pre-commit:
  commands:
    - cargo fmt --all -- --check
    - cargo clippy --workspace --release --all-targets --all-features -- -D warnings
    - cargo audit
    - cargo outdated

pre-push:
  commands:
    - cargo test -q
    - cargo build -q
